---
- name: Install Base Packages
  include_role:
    name: _packages
    tasks_from: "{{ item }}"
  loop:
    - mas
    - iterm
    - xcode
    - pass
    - authy
    - bitwarden
    - launchctl
    - diskinventory

- name: Base Packages Done
  ansible.builtin.set_fact:
    base_packages: done