---

# specific changes for MacOS

- name: Check if bash-completion is in place.
  shell: "test -f {{ brew_root}}/etc/bash_completion && echo 'yes'"
  environment:
    PATH: "/usr/local/bin:{{ ansible_env.USER }}/bin:{{ ansible_env.PATH }}"
  register: completion

- name: Add completions to .profile
  when:
    - ansible_distribution == "MacOSX"
    - ansible_pkg_mgr == "homebrew"
    - completion
  blockinfile:
    path: "{{ ansible_env.HOME }}/.profile"
    block: |
      if [ -f $(brew --prefix)/etc/bash_completion ]; then
         . $(brew --prefix)/etc/bash_completion
      fi

